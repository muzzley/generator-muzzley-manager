# Please edit to the right path of the logs and then
# Copy this file to /etc/logrotate.d/manager-[packageName]

{logPath}/manager-[packageName]/*.log {
  weekly
  missingok
  rotate 5
  compress
  delaycompress
  notifempty
  copytruncate
}
